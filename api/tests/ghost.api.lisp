(defpackage ghost.api-test
  (:use :cl
        :ghost.api
        :prove))
(in-package :ghost.api-test)

;; NOTE: To run this test file, execute `(asdf:test-system :ghost.api)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)

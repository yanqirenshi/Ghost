(defpackage ghost.graph-test
  (:use :cl
        :ghost.graph
        :prove))
(in-package :ghost.graph-test)

;; NOTE: To run this test file, execute `(asdf:test-system :ghost.graph)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
